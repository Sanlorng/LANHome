<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
                                                   xmlns:app="http://schemas.android.com/apk/res-auto"
                                                   android:orientation="vertical"
                                                   android:layout_width="match_parent"
                                                   android:layout_height="120dp">
    <ImageView
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:id="@+id/avatarDrawer"
            android:layout_marginStart="@dimen/margin_horizon"
            android:layout_width="?attr/actionBarSize"
            android:layout_height="?attr/actionBarSize" />
    <TextView
            app:layout_constraintStart_toEndOf="@id/avatarDrawer"
            app:layout_constraintTop_toTopOf="@id/avatarDrawer"
            app:layout_constraintBottom_toBottomOf="@id/avatarDrawer"
            android:id="@+id/userNameDrawer"
            android:layout_marginStart="@dimen/margin_horizon"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />
</androidx.constraintlayout.widget.ConstraintLayout>